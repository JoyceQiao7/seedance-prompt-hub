# X API v2 Bearer Token (Recent Search). Create a Project + App at https://developer.x.com
TWITTER_BEARER_TOKEN=

# Optional: refines extracted prompts and filters noise (gpt-4o-mini by default)
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
